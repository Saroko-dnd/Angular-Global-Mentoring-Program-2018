import { AuthorsSelectorValidatorDirective } from './authors-selector-validator.directive';

describe('AuthorsSelectorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorsSelectorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
